# alex-mine-corrections

Mine local Cursor transcripts → principle candidates (never auto-upsert).

```bash
python3 ${CLAUDE_PLUGIN_ROOT}/bin/mine-corrections --strong-only $ARGUMENTS
# review docs/memory/mining/principle-candidates.md then principle-upsert accepted ids only
```
