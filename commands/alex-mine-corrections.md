# alex-mine-corrections

Mine local Cursor transcripts and auto-upsert named-cluster principles (`--no-apply` to skip writes).

```bash
python3 ${CLAUDE_PLUGIN_ROOT}/bin/mine-corrections --strong-only $ARGUMENTS
```
