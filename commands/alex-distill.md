# alex-distill

When L0 overflows, list largest entries and forget/done to shrink (never silent-truncate).

```bash
python3 ${CLAUDE_PLUGIN_ROOT}/bin/distill
python3 ${CLAUDE_PLUGIN_ROOT}/bin/principle-forget --id P-…
```
