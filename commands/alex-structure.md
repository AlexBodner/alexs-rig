# alex-structure

Always-on codebase graph: query understand-anything / codemap before grep.

```bash
python3 ${CLAUDE_PLUGIN_ROOT}/bin/graph-status
```

If missing: `/understand --auto-update` and/or `/codemap-py:scan-codebase`. See `rules/knowledge-graph.md`.
