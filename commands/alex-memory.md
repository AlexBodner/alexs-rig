# alex-memory

Park todos / update progress / upsert principles via Alex's Rig CLIs. See skill `alex-memory`.

```bash
python3 ${CLAUDE_PLUGIN_ROOT}/bin/l0-show
python3 ${CLAUDE_PLUGIN_ROOT}/bin/pending-upsert upsert --id T-… --priority P1 --text "…"
python3 ${CLAUDE_PLUGIN_ROOT}/bin/principle-upsert --id P-… --text "…"
python3 ${CLAUDE_PLUGIN_ROOT}/bin/l0-regen
```
